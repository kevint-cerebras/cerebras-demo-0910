import { deliverySlots, getStore, productsFor } from "../shared/catalog";
import { foodArt } from "../shared/art";
import type { StoreId } from "../shared/types";
const escapeHtml = (s: string) =>
  s.replace(
    /[&<>"']/g,
    (c) =>
      ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[
        c
      ]!,
  );
export function shopHTML(storeId: StoreId) {
  const store = getStore(storeId);
  const products = productsFor(storeId).map((p) => ({
    ...p,
    artwork: foodArt(p.art, p.name, store.color),
  }));
  const payload = JSON.stringify({
    store,
    products,
    slots: deliverySlots(),
  }).replace(/</g, "\\u003c");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${store.shortName} · Grocery sandbox</title><style>
  *{box-sizing:border-box}body{margin:0;background:#fffefa;color:#273a2e;font-family:'Avenir Next',Avenir,'Trebuchet MS',sans-serif;font-size:14px}button,input,select{font:inherit}button{cursor:pointer}button:disabled{cursor:default;opacity:.45}.top-note{height:29px;background:${store.color};color:#fff;text-align:center;font-size:10px;letter-spacing:.8px;padding:8px}header{padding:23px 34px 19px;display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid #e9e8de}.logo{font-family:Georgia,serif;font-size:31px;letter-spacing:-1.4px;font-weight:bold;color:${store.color}}.logo i{font-style:normal;display:inline-block;width:15px;height:22px;background:${store.color};border-radius:15px 0 15px 0;margin-right:9px;transform:rotate(15deg)}.location{font-size:11px;color:#80877c;margin-top:5px}.cart-button{border:1px solid #dbe1d4;background:${store.light};color:${store.color};padding:11px 16px;border-radius:24px;font-size:12px;font-weight:700}.nav{display:flex;gap:25px;padding:14px 34px;border-bottom:1px solid #e9e8de;font-size:11px;color:#758071}.nav span:first-child{font-weight:bold;color:${store.color}}main{padding:25px 34px}.search{display:flex;gap:8px;margin-bottom:24px}.search input{width:100%;border:1px solid #dadfd2;border-radius:8px;background:#fff;padding:12px 15px;outline-color:${store.color}}.search button{background:${store.color};color:white;border:0;padding:0 23px;border-radius:8px}.banner{background:${store.light};border-radius:12px;padding:24px 29px;margin-bottom:23px;position:relative;overflow:hidden}.banner h1{font-family:Georgia,serif;font-weight:400;margin:0 0 8px;font-size:32px;letter-spacing:-.7px}.banner p{font-size:12px;color:#6c7b60;margin:0}.banner .small{font-size:9px;letter-spacing:1.5px;font-weight:700;margin-bottom:9px}.banner-art{position:absolute;width:150px;right:40px;top:-25px;transform:rotate(15deg)}.section-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:15px}.section-head h2{font-size:17px;font-weight:600;margin:0}.section-head span{color:#7c8676;font-size:11px}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.product{min-width:0;position:relative}.product-art{height:150px;border-radius:10px;display:flex;align-items:center;justify-content:center;position:relative}.product-art svg{width:143px;height:143px}.product .brand{font-size:8px;text-transform:uppercase;letter-spacing:1px;color:#8a8e7b;margin:12px 0 5px}.product h3{font-size:12px;line-height:1.4;font-weight:600;margin:0 0 5px}.size{font-size:10px;color:#8c9183}.bottom{display:flex;align-items:center;justify-content:space-between;margin:10px 0}.price{font-size:13px;font-weight:700}.add{border:1px solid #dce4d4;border-radius:20px;background:white;color:${store.color};width:29px;height:29px;font-size:20px;line-height:1}.add[data-added=true]{background:${store.color};color:white;border-color:${store.color}}.diet-label{font-size:8px;letter-spacing:.3px;color:#738061;min-height:11px}.ingredients{font-size:10px;line-height:1.5;color:#66725c;margin:8px 0}.ingredients summary{cursor:pointer}.tag{position:absolute;top:10px;left:10px;padding:4px 6px;background:#fffdf0;border-radius:3px;font-size:8px;color:${store.color}}.cart-view{display:none}.cart-view.active{display:block}.browse.hidden{display:none}.cart-header{display:flex;align-items:center;justify-content:space-between}.cart-header h1{font-family:Georgia,serif;font-size:28px;font-weight:400}.back{border:0;background:transparent;color:${store.color};padding:8px 0}.cart-list{display:flex;flex-direction:column;gap:7px}.cart-row{display:flex;align-items:center;border-bottom:1px solid #e7e9df;padding:6px 0;gap:14px}.cart-row .thumb{width:64px;height:61px;border-radius:8px;flex-shrink:0}.cart-row svg{width:64px;height:61px}.cart-row h3{font-size:12px;margin:0 0 5px}.cart-row .info{flex:1}.cart-row input{width:48px;padding:7px;border:1px solid #dadfd2;border-radius:5px;font-size:12px}.cart-row strong{font-size:12px;width:65px;text-align:right}.checkout{display:grid;grid-template-columns:1.15fr 1fr;gap:32px;margin-top:20px}.checkout h2{font-size:14px;margin:0 0 13px}.slot{display:block;padding:10px;border:1px solid #dfe5d8;border-radius:6px;margin:7px 0;font-size:11px}.slot:has(input:checked){background:${store.light};border-color:${store.color}}.slot input{accent-color:${store.color};margin-right:8px}.slot:has(input:disabled){color:#a6aa9e;background:#f3f3ee}.summary{padding:19px;background:#f2f4ec;border-radius:9px;height:fit-content}.summary div{display:flex;justify-content:space-between;font-size:12px;margin:8px 0}.summary .total{border-top:1px solid #d9dfd0;padding-top:13px;font-size:17px;font-weight:600}.purchase{margin-top:12px;background:${store.color};color:white;border:0;border-radius:7px;padding:13px;width:100%;font-size:12px;font-weight:600}.fine-print{font-size:9px;color:#859079;text-align:center;margin:10px 0 0}.empty{padding:50px 0;text-align:center;color:#7b8872}.order-success{padding:42px;text-align:center;background:${store.light};border-radius:12px;margin-top:25px}.order-success h2{font-family:Georgia,serif;font-size:27px;font-weight:400}.order-success p{font-size:12px;line-height:1.7}.search-query{font-family:Georgia,serif;font-size:26px;letter-spacing:-.5px}.empty-search{grid-column:1/-1;padding:40px;text-align:center;color:#7b8872}footer{padding:20px 34px;text-align:center;color:#9da28f;font-size:9px;border-top:1px solid #eceee4;margin-top:20px}button:focus-visible,input:focus-visible{outline:3px solid #eab574;outline-offset:3px}
  </style></head><body><div class="top-note">NEIGHBORHOOD GROCERIES, DELIVERED · COMMERCE SANDBOX</div><header><div><div class="logo"><i></i>${store.name}</div><div class="location">Your neighborhood · Demo delivery address</div></div><button class="cart-button" id="open-cart" aria-label="Open cart">Your basket <span id="cart-count">0</span></button></header><div class="nav"><span>Shop all</span><span>Produce</span><span>Pantry</span><span>Dairy & alternatives</span><span>Weekly favorites</span></div><main><section class="browse" id="browse"><form class="search" id="search-form"><input id="search" aria-label="Search groceries" placeholder="Search for something good…" autocomplete="off"><button type="submit" id="search-submit">Search</button></form><div class="banner" id="banner"><div class="small">FRESH PICKS, EVERY DAY</div><h1>${escapeHtml(store.subtitle)}</h1><p>A full fridge. One less thing on your list.</p><div class="banner-art">${foodArt("avocado")}</div></div><div class="section-head"><h2 id="result-title">A few everyday favorites</h2><span id="result-count"></span></div><div class="grid" id="products" data-ready="false"></div></section><section class="cart-view" id="cart-view"><div class="cart-header"><h1>Your basket</h1><button class="back" id="back-to-shop">← Continue shopping</button></div><div id="cart-list" class="cart-list"></div><div class="checkout" id="checkout"><div><h2>Choose your delivery</h2><div id="slots"></div></div><div class="summary"><div><span>Subtotal</span><span id="subtotal">$0.00</span></div><div><span>Delivery</span><span id="delivery">$0.00</span></div><div><span>Tax · sandbox groceries</span><span>$0.00</span></div><div class="total"><span>Total</span><span id="total">$0.00</span></div><button class="purchase" id="place-order" disabled>Place sandbox order</button><p class="fine-print">Sandbox only. No charge or real delivery.</p></div></div><div id="receipt"></div></section></main><footer>${store.shortName} is a fictional store. Prices, stock, ingredient labels and delivery windows are demo data.</footer><script>
  const data=${payload};
  const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n/100);
  const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let cart=JSON.parse(sessionStorage.getItem('cart')||'{}'), selectedSlot=sessionStorage.getItem('slot')||'', ordered=false;
  const save=()=>{sessionStorage.setItem('cart',JSON.stringify(cart));sessionStorage.setItem('slot',selectedSlot)};
  function renderProducts(query=''){
    const q=query.toLowerCase().trim();const filtered=data.products.filter(p=>!q||p.key===q||p.name.toLowerCase().includes(q));
    const shown=q?filtered:data.products.slice(0,8);
    document.getElementById('banner').style.display=q?'none':'';
    document.getElementById('result-title').textContent=q?'Results for “'+q.replaceAll('-',' ')+'”':'A few everyday favorites';
    document.getElementById('result-count').textContent=shown.length+' products';
    document.getElementById('products').innerHTML=shown.map(p=>'<article class="product" data-product="'+escape(p.id)+'" data-json="'+escape(JSON.stringify(p))+'"><div class="product-art" style="background:'+p.color+'">'+p.artwork+(p.diets.includes('vegan')?'<span class="tag">PLANT BASED</span>':'')+'</div><div class="brand">'+escape(p.brand)+'</div><h3>'+escape(p.name)+'</h3><div class="size">'+escape(p.size)+'</div><div class="bottom"><span class="price">'+money(p.price)+'</span><button class="add" data-add="'+p.id+'" data-added="'+Boolean(cart[p.id])+'" aria-label="Add '+escape(p.name)+'" '+(!p.stock?'disabled':'')+'>'+(cart[p.id]?'✓':'+')+'</button></div><div class="diet-label">'+p.diets.filter(d=>['vegan','gluten-free'].includes(d)).map(escape).join(' · ')+'</div><details class="ingredients"><summary>Ingredients & allergens</summary><p>'+escape(p.ingredients)+'</p><p>Contains: '+escape(p.allergens.join(', ')||'none declared')+'</p><p>May contain: '+escape(p.mayContain.join(', ')||'none declared')+'</p></details></article>').join('')||'<p class="empty-search">No matching products.</p>';
    document.getElementById('products').dataset.ready='true';document.getElementById('products').dataset.query=q;
  }
  function renderCart(){
    const count=Object.values(cart).reduce((a,b)=>a+b,0);document.getElementById('cart-count').textContent=count;
    document.getElementById('cart-count').dataset.count=count;
    const rows=Object.entries(cart).filter(([id,n])=>n>0).map(([id,quantity])=>({product:data.products.find(p=>p.id===id),quantity}));
    document.getElementById('cart-list').innerHTML=rows.map(({product:p,quantity:q})=>'<div class="cart-row" data-cart-product="'+p.id+'" data-quantity="'+q+'" data-price="'+p.price+'"><div class="thumb" style="background:'+p.color+'">'+p.artwork+'</div><div class="info"><h3>'+escape(p.name)+'</h3><div class="size">'+escape(p.size)+'</div></div><input type="number" min="1" max="12" aria-label="Quantity for '+escape(p.name)+'" data-quantity-input="'+p.id+'" value="'+q+'"><strong>'+money(p.price*q)+'</strong></div>').join('')||'<p class="empty">Your next good meal starts here.</p>';
    document.getElementById('slots').innerHTML=data.slots.map(s=>'<label class="slot" data-slot="'+s.id+'" data-json="'+escape(JSON.stringify(s))+'"><input type="radio" name="delivery" value="'+s.id+'" '+(s.id===selectedSlot?'checked ':'')+(!s.available?'disabled ':'')+'>'+escape(s.label)+(s.fee?' · +'+money(s.fee):'')+(!s.available?' · Full':'')+'</label>').join('');
    const subtotal=rows.reduce((a,r)=>a+r.product.price*r.quantity,0), slot=data.slots.find(s=>s.id===selectedSlot), delivery=Math.round(data.store.deliveryFee*100)+(slot?.fee||0);
    document.getElementById('subtotal').textContent=money(subtotal);document.getElementById('subtotal').dataset.cents=subtotal;
    document.getElementById('delivery').textContent=money(delivery);document.getElementById('delivery').dataset.cents=delivery;
    document.getElementById('total').textContent=money(subtotal+delivery);document.getElementById('total').dataset.cents=subtotal+delivery;
    document.getElementById('place-order').disabled=!rows.length||!slot||ordered;save();
  }
  document.getElementById('search-form').addEventListener('submit',e=>{e.preventDefault();renderProducts(document.getElementById('search').value)});
  document.getElementById('products').addEventListener('click',e=>{const button=e.target.closest('[data-add]');if(!button||button.disabled)return;cart[button.dataset.add]=(cart[button.dataset.add]||0)+1;renderCart();button.textContent='✓';button.dataset.added='true'});
  document.getElementById('open-cart').addEventListener('click',()=>{document.getElementById('browse').classList.add('hidden');document.getElementById('cart-view').classList.add('active');renderCart()});
  document.getElementById('back-to-shop').addEventListener('click',()=>{document.getElementById('browse').classList.remove('hidden');document.getElementById('cart-view').classList.remove('active')});
  document.getElementById('slots').addEventListener('change',e=>{if(e.target.matches('input[name=delivery]')){selectedSlot=e.target.value;renderCart()}});
  document.getElementById('cart-list').addEventListener('change',e=>{const id=e.target.dataset.quantityInput;if(id){cart[id]=Math.max(1,Math.min(12,Number(e.target.value)||1));renderCart()}});
  document.getElementById('place-order').addEventListener('click',()=>{if(ordered||!selectedSlot)return;ordered=true;document.getElementById('place-order').disabled=true;const id='DASH-'+crypto.randomUUID().slice(0,8).toUpperCase();document.getElementById('receipt').innerHTML='<div class="order-success" data-receipt="'+id+'"><h2>Consider dinner handled.</h2><p>Sandbox order '+id+'<br>'+escape(data.slots.find(s=>s.id===selectedSlot).label)+'<br>No payment was taken. No real delivery will be made.</p></div>';document.getElementById('checkout').style.display='none'});
  renderProducts();renderCart();
  </script></body></html>`;
}
